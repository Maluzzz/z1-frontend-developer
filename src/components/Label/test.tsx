import '@testing-library/jest-dom'
import { render, screen } from '@testing-library/react'

import { Label } from './index'

describe('Testing component Label', () => {
  test('should return the component with danger type', () => {
    return
  })
  test('should return the component with success type', () => {
    return
  })
  test('should return the empty component', () => {
    return
  })
})
